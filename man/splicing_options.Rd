% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-splicing_options.R
\name{splicing_options}
\alias{splicing_options}
\title{Analysis options for splicing and sequence maps}
\usage{
splicing_options(
  width_exon = 50,
  width_intron = 250,
  event_fdr = 0.05,
  control_pval = 0.95,
  psi_cutoff = c(-0.1, 0.1),
  psi_control_max = 0.005,
  min_count = 50,
  groups = c("Negative", "Positive", "Control"),
  control_multiplier = 2,
  control_iterations = 20,
  moving_average = 10,
  stat_test = "fisher",
  use_fdr = TRUE,
  fdr_threshold = 0.05,
  verbose = FALSE
)
}
\arguments{
\item{width_exon}{Integer. Bp to extend into exons from each junction.}

\item{width_intron}{Integer. Bp to extend into introns from each junction.}

\item{event_fdr}{Maximum value of the rMATS \code{FDR} column for an event to
be eligible as Positive or Negative.}

\item{control_pval}{Minimum value of the rMATS \code{PValue} column for an
event to be eligible as a Control.}

\item{psi_cutoff}{Length-2 numeric \code{c(neg, pos)} giving the
\code{IncLevelDifference} thresholds. Events with \code{ΔΨ < neg} are Negative,
\code{ΔΨ > pos} are Positive.}

\item{psi_control_max}{Maximum \code{abs(IncLevelDifference)} allowed for an
event to enter the Control pool.}

\item{min_count}{Minimum junction-read coverage required for an event to
enter the analysis. Each rMATS sample must have
\code{IJC + SJC > min_count}, where per-replicate counts inside each cell
(comma-separated) are summed before comparison. \code{0} or \code{NULL} disables
the filter. When enabled, \code{events} must contain
\code{IJC_SAMPLE_1}, \code{SJC_SAMPLE_1}, \code{IJC_SAMPLE_2}, \code{SJC_SAMPLE_2}.}

\item{groups}{Character subset of \code{c("Negative", "Positive", "Control")}
specifying which event groups to compute.}

\item{control_multiplier}{Numeric multiplier for control sample size.
Controls drawn per iteration is
\code{(n_positive + n_negative) * control_multiplier}.}

\item{control_iterations}{Integer number of bootstrap iterations for the
control distribution.}

\item{moving_average}{Window size for moving-average smoothing.
\code{NULL} or \code{0} disables smoothing.}

\item{stat_test}{Per-position significance test for Positive / Negative
vs. Control. One of \code{"fisher"} (Fisher's exact test) or \code{"binomial"}.}

\item{use_fdr}{If \code{TRUE}, apply Benjamini-Hochberg FDR correction to the
per-position p-values before thresholding. If \code{FALSE}, threshold the raw
p-values.}

\item{fdr_threshold}{Significance threshold applied to the (FDR-corrected
or raw) p-values.}

\item{verbose}{If \code{TRUE}, print progress messages.}
}
\value{
A named list of validated analysis options.
}
\description{
Returns a validated list of options controlling event filtering, region
geometry, control bootstrap, moving average, and significance testing for
splicing-map / sequence-map entry points.
}
\seealso{
Other params: 
\code{\link{peaks_options}()},
\code{\link{peaks_plot_style}()},
\code{\link{splicing_style}()},
\code{\link{utr_style}()}
}
\concept{params}
