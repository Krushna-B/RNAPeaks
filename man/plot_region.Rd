% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_region.R
\name{plot_region}
\alias{plot_region}
\title{Plot RBP peaks over a multi-gene genomic region}
\usage{
plot_region(
  bed,
  chr,
  start,
  end,
  strand,
  gtf = NULL,
  species = "hg38",
  bam_files = NULL,
  peaks_opts = peaks_options(),
  style = peaks_plot_style()
)
}
\arguments{
\item{bed}{Validated BED data frame or named list of BED data frames.}

\item{chr}{Chromosome (with or without \code{"chr"} prefix; normalized).}

\item{start, end}{Region bounds (bp, \code{start <= end}).}

\item{strand}{\code{"+"} or \code{"-"}.}

\item{gtf}{Optional path to a local GTF file. If \code{NULL}, the bundled
annotation for \code{species} is used.}

\item{species}{One of \code{"hg38"}, \code{"mm10"}, or \code{"mm39"}. Ignored when
\code{gtf} is supplied.}

\item{bam_files}{Optional named character vector of BAM file paths drawn as
coverage tracks above the gene structure. Names become track labels; if
unnamed, the filename (without extension) is used. Each BAM must be
sorted and have a \code{.bai} index next to it.}

\item{peaks_opts}{Output of \code{\link[=peaks_options]{peaks_options()}}: BED filtering / ordering options.}

\item{style}{Output of \code{\link[=peaks_plot_style]{peaks_plot_style()}}: visual settings.}
}
\value{
A ggplot object.
}
\description{
Selects every gene whose body overlaps the window \code{chr:start-end} on
\code{strand} (one transcript per gene, longest by default), stacks their
structures vertically, and overlays peaks from \code{bed}.
}
\seealso{
Other plot: 
\code{\link{plot_gene}()},
\code{\link{plot_utr_binding}()}
}
\concept{plot}
