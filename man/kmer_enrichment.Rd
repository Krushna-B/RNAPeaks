% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kmer_enrichment.R
\name{kmer_enrichment}
\alias{kmer_enrichment}
\title{Compare k-mer enrichment between two sets of sequences}
\usage{
kmer_enrichment(
  set_a,
  set_b,
  k,
  gtf = NULL,
  species = "hg38",
  genome = NULL,
  label_a = "Set A",
  label_b = "Set B",
  top_n = 20,
  title = ""
)
}
\arguments{
\item{set_a, set_b}{The two sets to compare. Each is a BED data frame, a list
of BED data frames, a \code{.bed} path, or a character vector of gene /
transcript ids.}

\item{k}{K-mer length.}

\item{gtf}{Optional path to a local GTF file. If \code{NULL}, the bundled
annotation for \code{species} is used}

\item{species}{One of \code{"hg38"}, \code{"mm10"}, or \code{"mm39"}. Selects both the
bundled GTF and the default genome. Ignored where \code{gtf} / \code{genome} are
supplied.}

\item{genome}{A BSgenome object or a genome key (\code{"hg38"} / \code{"mm10"} /
\code{"mm39"}) used for sequence extraction. Defaults to \code{species}.}

\item{label_a, label_b}{Display names for the two sets, used in the table
context and plot axes.}

\item{top_n}{Number of most strongly enriched k-mers (by absolute
difference) to label in the scatter plot}

\item{title}{Plot title.}
}
\value{
A list with \code{table} (a data frame sorted by \code{difference}, one row per
k-mer with \code{kmer}, \code{freq_a}, \code{freq_b}, \code{count_a}, \code{count_b}, \code{difference},
and \code{rank}) and \code{plots} (a list of two ggplots: \code{scatter} and \code{rank}).
}
\description{
Counts every length-\code{k} k-mer across two sets of sequences, normalizes each
set to a relative frequency, and reports the per-k-mer difference
\code{freq_a - freq_b}. Each set may be gene ids, transcript ids, BED peaks, or a
mix of gene / transcript ids.
}
