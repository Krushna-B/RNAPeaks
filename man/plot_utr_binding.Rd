% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_utr_binding.R
\name{plot_utr_binding}
\alias{plot_utr_binding}
\title{Plot RBP peak binding density over 5' and 3' UTRs}
\usage{
plot_utr_binding(
  bed,
  gtf = NULL,
  transcripts = NULL,
  species = "hg38",
  moving_average = 5L,
  style = utr_style(),
  track_colors = NULL,
  title = ""
)
}
\arguments{
\item{bed}{Validated BED data frame, BED file path, or named list of
either; one density curve is drawn per element.}

\item{gtf}{Optional GTF file path. If \code{NULL}, the bundled
annotation for \code{species} is used.}

\item{transcripts}{Optional character vector of transcript ids to
restrict to. Default uses every protein-coding transcript.}

\item{species}{One of \code{"hg38"}, \code{"mm10"}, \code{"mm39"}. Ignored when
\code{gtf} is supplied.}

\item{moving_average}{Window size for moving-average smoothing of the
per-bin curve. \code{0} or \code{NULL} disables smoothing.}

\item{style}{Output of \code{\link[=utr_style]{utr_style()}}.}

\item{track_colors}{Optional named character vector of colors keyed
by BED track name. When \code{NULL}, a single track is drawn black and
multiple tracks use a \code{Set1} palette.}

\item{title}{Plot title.}
}
\value{
A list with \code{plot} (ggplot) and \code{data} (per-bin density frame).
}
\description{
For every protein-coding gene in \code{gtf} (or the subset named by
\code{transcripts}), picks the transcript with the longest exonic 5' UTR
and the transcript with the longest exonic 3' UTR. Peaks from \code{bed}
that fall the UTR are binarized per-bp on the
spliced transcript, resampled into 100 bins per side, and averaged
across genes. The result is two density curves (one per BED track)
shown left-to-right as \verb{[5' UTR] // [3' UTR]}.
}
\seealso{
Other plot: 
\code{\link{plot_gene}()},
\code{\link{plot_region}()}
}
\concept{plot}
